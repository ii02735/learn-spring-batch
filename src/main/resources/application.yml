spring:
  application:
    name: learning_spring_batch
  datasource:
    url: jdbc:h2:file:./src/main/resources/spring-batch-h2
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  batch:
    jdbc:
      initialize-schema: always
